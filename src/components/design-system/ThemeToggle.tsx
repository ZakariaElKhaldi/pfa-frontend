'use client'

/**
 * Theme is fixed to light (glassmorphism). This component is kept as a stub
 * so any existing Storybook stories that import it continue to compile.
 */
export function ThemeToggle() {
  return null
}
