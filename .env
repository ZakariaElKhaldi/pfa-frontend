# Local dev env for frontend (gitignored — do not commit).
# See .env.example for the committed template.

VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000

# Set to true to skip the login page entirely in local dev (uses a mock admin user).
# Set to false or remove when testing real auth flows.
VITE_DEV_BYPASS_AUTH=false
